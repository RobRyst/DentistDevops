name: "CodeQL"

on:
  push:
    branches: ["dev"]
  pull_request:
    branches: ["dev"]
  schedule:
    - cron: "0 2 * * 1"

jobs:
  analyze:
    name: AnalyzeCode
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
